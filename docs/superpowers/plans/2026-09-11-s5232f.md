# Section 2.3 — S5232F implementation plan

Goal: implement the user's Section 2.3 specification in the existing viewer only.
Architecture: reuse Geometry/metadata from xe7745 without editing it; reuse ModelAsset,
modelInstance, component/focus registries, static repository and existing store.
User's detailed Section 2.3 specification authorizes this implementation. Work in the
current working tree to preserve the uncommitted Section 2.2 dependencies.

- [x] Add failing mapping, repository, selection-isolation and Inspector tests.
- [x] Add S5232F component template; extend mapper, registry and switch selection guards.
- [x] Extend PowerSwitch, Inspector and camera while preserving server state behavior.
- [x] Generate bottom-center XYZ-meter chassis, I/O and PSU side with linked port/fan meshes.
- [x] Validate exact counts, dimensions, names, exported transforms/extras and budget.
- [x] Execute Blender twice; verify deterministic GLB and source-only save.
- [x] Run TypeScript, tests, lint and production build; inspect runtime and fallback.
- [x] Write beginner instructions and measured verification report; stop for visual approval.

Constraints: one GLB for U44/U43; chassis 0.434 × 0.460 × 0.0436 m;
32 QSFP28 + 2 SFP+, 2 PSU, 4 fans; no telemetry, cables, rack model or server edits.
Dell guide pages 8, 10, 12–13 establish layout and dimensions. Printed numbers are
simplified stable physical IDs; no switch-OS interface naming is asserted.
